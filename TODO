 * Watch children process using "context.watch(actorRef)"
 	def receive = {
		case Terminated(children) => 
 * Add deploy task in build.gradle
 * Execute generateFmppSources only if needed (Gradle)
 * Adjust dispatcher parameters
 * Add supervision interface (webui ?)
 * Implement resume